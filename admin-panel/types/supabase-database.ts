/**
 * Источник: `supabase/functions/types.gen.ts` (`supabase gen types typescript`).
 * После регенерации при необходимости поправьте `Functions.set_client_assignment.Args`:
 * в Postgres `p_current_manager_id` допускает NULL (снятие назначения).
 */
export type { Database } from "../../supabase/functions/types.gen";
